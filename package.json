{
  "name": "nuxt-gh-pages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "core-js": "^3.15.1",
    "nuxt": "^2.15.7",
    "sass": "^1.42.1"
  },
  "devDependencies": {
    "node-sass": "^6.0.1",
    "push-dir": "^0.4.1",
    "sass-loader": "^10"
  }
}
